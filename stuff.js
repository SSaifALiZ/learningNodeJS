const counter = (arr) => `There are ${arr.length} elements in array`;

const adder = (a, b) => `The sum is ${a + b}`;

const pi = 1.142;

// module.exports.counter = counter;
// module.exports.adder = adder;
// module.exports.pi = pi;

module.exports = { counter, adder, pi };
